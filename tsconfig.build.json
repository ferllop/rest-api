{
    "extends": "./tsconfig.json",
    "exclude": [
      "tests/**/*"
    ],
    "compilerOptions": {
      "outDir": "./build",                              /* Redirect output structure to the directory. */
    }
  }
